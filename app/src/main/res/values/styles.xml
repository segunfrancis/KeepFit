<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="backgroundColor">@color/colorText</item>
        <item name="secondaryBackgroundColor">@color/colorPrimary</item>
        <item name="textColor">@color/colorBackground</item>
        <item name="secondaryTextColor">@color/colorSecondaryAccent</item>
        <item name="tertiaryTextColor">@color/colorText</item>
        <item name="editText">@drawable/edit_text_background</item>
        <item name="android:windowAnimationStyle">@style/WindowAnimationTransition</item>
    </style>

    <!-- Dark application theme. -->
    <style name="NightTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="colorPrimary">@color/colorSecondary</item>
        <item name="colorPrimaryDark">@color/colorSecondaryDark</item>
        <item name="colorAccent">@color/colorSecondaryAccent</item>
        <item name="backgroundColor">@color/colorBackground</item>
        <item name="secondaryBackgroundColor">@color/colorSecondary</item>
        <item name="textColor">@color/colorText</item>
        <item name="secondaryTextColor">@color/colorAccent</item>
        <item name="tertiaryTextColor">@color/colorText</item>
        <item name="editText">@drawable/edit_text_background_dark</item>
        <item name="android:windowAnimationStyle">@style/WindowAnimationTransition</item>
    </style>

    <style name="WindowAnimationTransition">
        <item name="android:windowEnterAnimation">@android:anim/fade_in</item>
        <item name="android:windowExitAnimation">@android:anim/fade_out</item>
    </style>
</resources>
